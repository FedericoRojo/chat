<html>
  <head>
    <title>Homepage</title>
    <link rel="stylesheet" href="./styles.css">
  </head>
  <body>
    <%- include('header', {links: links})%>
    <ul>
        <% messages.forEach((m) => { %>
          <%- include('message', {message: m}); %>
        <% }); %>
    </ul>
  </body>
</html>
